name: watchdog_app
description: "Script renderer tool."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.1
  process_run: ^0.12.2+1
  shared_preferences: ^2.0.15
  cupertino_icons: ^1.0.8
  file_picker: ^5.0.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
